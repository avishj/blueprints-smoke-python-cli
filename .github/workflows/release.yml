# SPDX-FileCopyrightText: 2026 Avish J <avish.j@pm.me>
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
name: Release

on:
  push:
    tags: ["v*"]

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions: {}

jobs:
  check:
    name: Check Published State
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      pypi: ${{ steps.check.outputs.pypi }}
      ghcr: ${{ steps.check.outputs.ghcr }}
      dockerhub: ${{ steps.check.outputs.dockerhub }}
      release: ${{ steps.check.outputs.release }}
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5  # v2.19.3
        with:
          egress-policy: audit

      - name: Check all targets
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ github.ref_name }}
          REPOSITORY: ${{ github.repository }}
        run: |
          V="${VERSION#v}"
          exists() { "$@" > /dev/null 2>&1 && echo true || echo false; }

          pypi_exists=$(exists curl -sf "https://pypi.org/pypi/blueprints-smoke-python-cli/$V/json")
          echo "pypi=$pypi_exists" >> "$GITHUB_OUTPUT"

          ghcr_exists=$(exists docker manifest inspect "ghcr.io/${REPOSITORY}:$V")
          echo "ghcr=$ghcr_exists" >> "$GITHUB_OUTPUT"

          dockerhub_exists=$(exists curl -sf "https://hub.docker.com/v2/repositories/${REPOSITORY}/tags/$V")
          echo "dockerhub=$dockerhub_exists" >> "$GITHUB_OUTPUT"

          release_exists=$(exists gh release view "$VERSION" --repo "${REPOSITORY}")
          echo "release=$release_exists" >> "$GITHUB_OUTPUT"

  build:
    name: Build Distribution
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      id-token: write  # sign provenance
      attestations: write  # attest distributions
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Setup
        uses: avishj/blueprints/stacks/python-cli/actions/setup@19bdb966ebc356d5b517946bc707c4d9e07f8361
        with:
          setup-python: "false"
          fetch-depth: 0

      - name: Verify tag is on main
        run: |
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "::error::Tag must point to a commit on the main branch."
            exit 1
          fi

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          enable-cache: false
          python-version: "3.13"

      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Verify tag matches pyproject.toml
        run: |
          TAG_VERSION="${GITHUB_REF#refs/tags/v}"
          PKG_VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "::error::Tag version ($TAG_VERSION) ≠ pyproject.toml version ($PKG_VERSION)"
            exit 1
          fi

      - name: Verify changelog entry exists
        run: |
          V="${GITHUB_REF#refs/tags/v}"
          VE=$(printf '%s' "$V" | sed 's/\./\\./g')
          if ! grep -qE "^##\s.*${VE}(\s|$)" CHANGELOG.md; then
            echo "::error::CHANGELOG.md missing entry for version $V"
            exit 1
          fi

      - name: Build sdist and wheel
        run: uv build

      - name: Attest build provenance
        uses: actions/attest@59d89421af93a897026c735860bf21b6eb4f7b26  # v4.1.0
        with:
          subject-path: dist/*

      - name: Upload dist
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: dist
          path: dist/
          retention-days: 90
          if-no-files-found: error

  publish-pypi:
    name: Publish to PyPI
    needs: [build, check]
    if: needs.check.outputs.pypi != 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      id-token: write  # publish to PyPI
    environment:
      name: pypi
      url: https://pypi.org/p/blueprints-smoke-python-cli

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5  # v2.19.3
        with:
          egress-policy: audit

      - name: Download dist
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b  # v1.14.0
        with:
          print-hash: true
          attestations: true

  sbom:
    name: Generate SBOM
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      id-token: write  # sign SBOM provenance
      attestations: write  # attest SBOM
    steps:
      - name: Setup
        uses: avishj/blueprints/stacks/python-cli/actions/setup@19bdb966ebc356d5b517946bc707c4d9e07f8361
        with:
          setup-python: "false"

      - name: Download dist
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: dist
          path: dist/

      - name: Generate SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610  # v0.24.0
        with:
          path: dist/
          artifact-name: sbom-spdx.json
          output-file: sbom-spdx.json
          format: spdx-json

      - name: Attest SBOM
        uses: actions/attest@59d89421af93a897026c735860bf21b6eb4f7b26  # v4.1.0
        with:
          subject-path: "dist/*"
          sbom-path: sbom-spdx.json

      - name: Upload SBOM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sbom
          path: sbom-spdx.json
          retention-days: 90

  docker:
    name: Docker (${{ matrix.registry }})
    needs: [build, check]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      packages: write  # push GHCR image
      id-token: write  # sign image provenance
      attestations: write  # attest container image
    strategy:
      fail-fast: false
      matrix:
        include:
          - registry: ghcr.io
            image: ghcr.io/${{ github.repository }}
            check_key: ghcr
          - registry: docker.io
            image: docker.io/${{ github.repository }}
            check_key: dockerhub
    steps:
      - name: Check if already published
        id: skip
        if: needs.check.outputs[matrix.check_key] == 'true'
        run: echo "published=true" >> "$GITHUB_OUTPUT"

      - name: Setup
        if: '!steps.skip.outputs.published'
        uses: avishj/blueprints/stacks/python-cli/actions/setup@19bdb966ebc356d5b517946bc707c4d9e07f8361
        with:
          setup-python: "false"

      - name: Set up Docker Buildx
        if: '!steps.skip.outputs.published'
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd  # v4.0.0

      - name: Log in to GHCR
        if: matrix.registry == 'ghcr.io' && !steps.skip.outputs.published
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121  # v4.1.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        if: matrix.registry == 'docker.io' && !steps.skip.outputs.published
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121  # v4.1.0
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        if: '!steps.skip.outputs.published'
        id: meta
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf  # v6.0.0
        with:
          images: ${{ matrix.image }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest

      - name: Build and push
        if: '!steps.skip.outputs.published'
        id: push
        uses: docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f  # v7.1.0
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

      - name: Attest build provenance
        if: '!steps.skip.outputs.published'
        uses: actions/attest@59d89421af93a897026c735860bf21b6eb4f7b26  # v4.1.0
        with:
          subject-name: ${{ matrix.image }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Install Cosign
        if: '!steps.skip.outputs.published'
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2

      - name: Sign image
        if: '!steps.skip.outputs.published'
        env:
          REPO: ${{ github.repository }}
          WORKFLOW: ${{ github.workflow }}
          REF: ${{ github.ref }}
          IMAGE: ${{ matrix.image }}@${{ steps.push.outputs.digest }}
        run: |
          cosign sign --yes --recursive \
            -a "repo=$REPO" \
            -a "workflow=$WORKFLOW" \
            -a "ref=$REF" \
            "$IMAGE"

      - name: Verify image signature & attestations
        if: '!steps.skip.outputs.published'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          WORKFLOW: ${{ github.workflow }}
          REF: ${{ github.ref }}
          SHA: ${{ github.sha }}
          TRIGGER: ${{ github.event_name }}
          IMAGE: ${{ matrix.image }}@${{ steps.push.outputs.digest }}
        run: |
          cosign verify \
            --certificate-identity "https://github.com/$REPO/.github/workflows/release.yml@$REF" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            --certificate-github-workflow-name "$WORKFLOW" \
            --certificate-github-workflow-repository "$REPO" \
            --certificate-github-workflow-ref "$REF" \
            --certificate-github-workflow-sha "$SHA" \
            --certificate-github-workflow-trigger "$TRIGGER" \
            "$IMAGE"
          gh attestation verify "oci://$IMAGE" --repo "$REPO"

      - name: Verify published image
        if: '!steps.skip.outputs.published'
        env:
          IMAGE: ${{ matrix.image }}@${{ steps.push.outputs.digest }}
        run: docker run --rm "$IMAGE" --help

  trivy:
    name: Trivy Image Scan
    needs: [build, docker]
    uses: ./.github/workflows/_trivy-image.yml
    with:
      image-ref: ghcr.io/${{ github.repository }}:${{ needs.build.outputs.version }}
    permissions:
      security-events: write  # upload SARIF results

  github-release:
    name: GitHub Release
    needs: [build, publish-pypi, sbom, docker, trivy, check]
    if: >-
      always()
      && needs.check.outputs.release != 'true'
      && needs.build.result == 'success'
      && needs.sbom.result == 'success'
      && needs.docker.result == 'success'
      && needs.trivy.result == 'success'
      && (needs.publish-pypi.result == 'success' || needs.check.outputs.pypi == 'true')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write  # create GitHub release
    steps:
      - name: Setup
        uses: avishj/blueprints/stacks/python-cli/actions/setup@19bdb966ebc356d5b517946bc707c4d9e07f8361
        with:
          setup-python: "false"
          sparse-checkout: CHANGELOG.md

      - name: Download dist
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: dist
          path: dist/

      - name: Download SBOM
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: sbom
          path: sbom/

      - name: Verify build attestations
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
        run: gh attestation verify dist/* --repo "${REPOSITORY}"

      - name: Collect image digests
        id: digests
        env:
          VERSION: ${{ needs.build.outputs.version }}
          REPOSITORY: ${{ github.repository }}
        run: |
          GHCR=$(docker buildx imagetools inspect "ghcr.io/${REPOSITORY}:${VERSION}" --format '{{json .Manifest}}' 2>/dev/null | jq -r '.digest // empty')
          DH=$(curl -sf "https://hub.docker.com/v2/repositories/${REPOSITORY}/tags/${VERSION}" | jq -r '.digest // empty' 2>/dev/null)
          echo "ghcr=${GHCR:-n/a}" >> "$GITHUB_OUTPUT"
          echo "dockerhub=${DH:-n/a}" >> "$GITHUB_OUTPUT"

      - name: Extract changelog
        id: changelog
        env:
          VERSION: ${{ needs.build.outputs.version }}
        run: |
          VE=$(printf '%s' "${VERSION}" | sed 's/\./\\./g')
          awk "/^## .*${VE}([[:space:]]|$)/{found=1; next} /^## /{if(found) exit} found" CHANGELOG.md > notes.md

      - name: Generate checksums
        run: |
          (cd dist && sha256sum ./*) > SHA256SUMS
          (cd sbom && sha256sum ./*) >> SHA256SUMS

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ needs.build.outputs.version }}
          REPOSITORY: ${{ github.repository }}
          REF_NAME: ${{ github.ref_name }}
          GHCR_DIGEST: ${{ steps.digests.outputs.ghcr }}
          DOCKERHUB_DIGEST: ${{ steps.digests.outputs.dockerhub }}
        run: |
          V="${VERSION}"

          GENERATED=$(gh api "repos/${REPOSITORY}/releases/generate-notes" \
            -f tag_name="${REF_NAME}" --jq '.body')

          cat >> notes.md <<EOF

          ---

          ${GENERATED}

          ## Install (uv recommended)

          \`\`\`
          uv add blueprints-smoke-python-cli==${V}
          \`\`\`

          or

          \`\`\`
          pip install blueprints-smoke-python-cli==${V}
          \`\`\`

          - [PyPI](https://pypi.org/project/blueprints-smoke-python-cli/${V}/)


          ## Docker Images

          - \`ghcr.io/${REPOSITORY}@${GHCR_DIGEST}\`
          - \`docker.io/${REPOSITORY}@${DOCKERHUB_DIGEST}\`
          EOF

          gh release create "${REF_NAME}" \
            --repo "${REPOSITORY}" \
            --verify-tag \
            --notes-file notes.md \
            dist/* sbom/* SHA256SUMS
